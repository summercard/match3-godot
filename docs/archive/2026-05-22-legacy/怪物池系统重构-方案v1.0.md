# 怪物池系统重构 - v1.0

## 版本目标

将当前以 `monster_id` 作为唯一怪物实体的存档模型，重构为以 `instanceId` 标识单只怪物实例的统一怪物池。

重构后：

- 同一种怪物可以拥有多只实例，每只实例独立记录等级、经验、性格、捕获时间、队伍位置和牧场放置状态。
- 牧场和组队编队共享同一套怪物实例数据。
- 图鉴回归物种展示，不再承担养成操作入口。
- 战斗读取队伍中的实例属性，战斗结算经验回写到对应实例。
- 建立统一的怪物管理机制，收束怪物模板、实例、数值、性格、进化和美术资产访问入口。
- 旧存档中的 `captured` + `pokedex` + `team` + `ranch` 可以自动迁移到新结构。

---

## 核心原则

### 1. 实例与模板分离

- `monsterId`：怪物模板 ID，对应 `MonsterDb.MONSTER_DB` 中的物种配置。
- `instanceId`：玩家持有的单只怪物实例 ID，是队伍、牧场、经验、进化的主键。

所有玩家养成相关操作都应优先使用 `instanceId`，只有展示静态物种信息时使用 `monsterId`。

### 2. 怪物池是实例数据唯一真源

`monster_pool` 存储每只怪物的成长状态。队伍和牧场只保存引用关系：

- `team.data.leader/member1/member2` 保存 `instanceId`
- `ranch.data.slots[].instance_id` 保存 `instanceId`
- `monster_pool[].monsterId` 才决定这只实例当前是什么物种

避免同时维护 `inTeam`、`inRanchSlot`、`team`、`ranch` 多份真源。必要时可以在运行时派生 `inTeam` / `inRanchSlot` 供 UI 使用。

### 3. 向后兼容旧接口，但新代码走新接口

为了降低一次性改造风险，`SaveManager` 可以保留旧的 `get_monster_level(monster_id)`、`add_monster_exp(monster_id)` 等方法作为兼容层，但新场景和新逻辑应使用实例接口。

### 4. 怪物信息统一入口

当前工程已经有 `MonsterDb`、`NatureDB`、`MonsterArtDB` 和 `SaveManager`，但 UI 和战斗代码到处直接拼装怪物数据。重构后应提供统一的怪物视图接口，让调用方不再关心数据来自模板、实例、性格还是资产映射。

推荐规则：

- `MonsterDb` 只负责静态模板和基础数值计算。
- `MonsterArtDB` 只负责怪物美术资产映射和 fallback。
- `MonsterPool` 负责玩家持有的实例数据。
- `SaveManager` 负责持久化、迁移和实例接口。
- 新增 `MonsterService` 或在 `MonsterPool` 中提供高层视图接口，统一返回 UI/战斗需要的完整怪物数据。

---

## 数据结构

### MonsterPool 数据格式

```gdscript
# section: player.data.monster_pool
# Array[Dictionary]
[
	{
		"instanceId": "m_1700000000000_ab12cd",
		"monsterId": "monster_001",
		"name": "小火龙",
		"level": 1,
		"exp": 0,
		"nature": "brave",
		"capturedAt": 1700000000000,
		"source": "starter",
		"favorite": false
	}
]
```

说明：

- 不在实例里缓存 `stats`。当前 `MonsterDb.get_monster_stats(monsterId, level, nature)` 已能计算最终属性，缓存会引入失效问题。若未来性能需要，再加运行时缓存。
- 不在实例里保存 `inTeam` / `inRanchSlot`。它们从 `team` 和 `ranch` 引用关系派生。
- `name` 默认为物种名，后续可支持改名。
- `source` 可选，用于区分 `starter`、`capture`、`reward`、`migration`。

### Team 数据格式

```gdscript
# section: team.data
{
	"leader": "m_1700000000000_ab12cd",
	"member1": "m_1700000001000_ef34gh",
	"member2": null
}
```

### Ranch 数据格式

```gdscript
# section: ranch.data
{
	"slots": [
		{ "instance_id": "m_1700000000000_ab12cd", "placed_at": 1700000000000 },
		{ "instance_id": null, "placed_at": null }
	],
	"unlocked_slots": 5
}
```

### 图鉴解锁派生规则

图鉴不再直接依赖旧 `captured` 数组。已捕获物种集合从怪物池派生：

```gdscript
captured_species = unique(monster_pool.map(func(m): return m["monsterId"]))
```

图鉴详情默认展示物种基础信息；如果已拥有该物种，则展示该物种第一只或最高等级实例的性格和实际数值，并可显示“已拥有 N 只”。

---

## SaveManager / MonsterPool 接口

### 新增文件

`src/core/monster_pool.gd`

职责：

- 生成实例 ID
- 创建、查询、筛选、更新、删除怪物实例
- 计算实例属性
- 处理旧存档迁移辅助逻辑

### SaveManager 新接口

```gdscript
func get_monster_pool() -> Array
func save_monster_pool(pool: Array) -> bool

func add_monster_instance(monster_id: String, options: Dictionary = {}) -> Dictionary
func get_monster_instance(instance_id: String) -> Dictionary
func update_monster_instance(instance_id: String, patch: Dictionary) -> bool
func remove_monster_instance(instance_id: String) -> bool

func get_owned_monsters(filters: Dictionary = {}) -> Array
func get_owned_species_ids() -> Array
func get_instances_by_monster_id(monster_id: String) -> Array

func get_instance_level(instance_id: String) -> int
func get_instance_exp(instance_id: String) -> int
func get_instance_nature(instance_id: String) -> String
func get_instance_stats(instance_id: String) -> Dictionary
func add_instance_exp(instance_id: String, exp_gained: int) -> Dictionary
func evolve_instance(instance_id: String) -> Dictionary
```

### 队伍接口

```gdscript
func load_team() -> Dictionary
func save_team(team_data: Dictionary) -> bool
func is_instance_in_team(instance_id: String) -> bool
func get_team_instances() -> Array
func get_team_battle_stats() -> Array
```

`load_team()` 继续返回 `{ leader, member1, member2 }`，但值改为 `instanceId`。

### 牧场接口

```gdscript
func get_ranch_state() -> Dictionary
func set_ranch_state(state: Dictionary) -> bool
func place_instance_in_ranch(instance_id: String, slot_index: int) -> bool
func remove_instance_from_ranch(instance_id: String) -> bool
func get_idle_exp_rate_for_instance(instance_id: String) -> float
func collect_idle_exp_for_instance(instance_id: String) -> float
```

---

## 统一怪物管理机制

### 当前问题

现状不是没有怪物系统，而是系统分散：

- `src/data/monster_db.gd`：静态怪物模板库，包含编号、名称、属性、稀有度、基础数值、技能、进化、Boss 技能等。
- `src/data/nature_db.gd`：性格数据库，被 `MonsterDb.get_monster_stats()` 用于数值修正。
- `src/data/monster_art_db.gd`：当前只有战斗头像映射，覆盖范围较少。
- `src/core/save_manager.gd`：用旧 `captured` / `pokedex` 记录玩家拥有和成长。
- 多个 UI 和战斗场景直接通过 `monster_id` 拼装数据。

这会造成几个问题：

- 无法区分同物种多只实例。
- 队伍、牧场、战斗、图鉴各自查数据，容易展示不一致。
- 美术资产映射不完整，且只有 battle portrait 一个维度。
- `monster_id` 同时承担模板 ID 和玩家怪物实体 ID，语义混乱。
- 后续新增怪物时，需要同时手动检查模板、资产、UI fallback、战斗读取等多处。

### 新增 `MonsterService`（推荐）

新增文件：

`src/core/monster_service.gd`

职责：

- 统一组装“怪物视图数据”。
- 将 `instanceId` 转成 UI/战斗可直接使用的 Dictionary。
- 对外隐藏 `MonsterDb`、`NatureDB`、`MonsterArtDB`、`SaveManager` 的组合细节。
- 提供资产 fallback 和数据校验入口。

如果希望减少新增文件，也可以先把这些高层函数放在 `MonsterPool` 中；但长期建议独立为 `MonsterService`。

### MonsterService 接口建议

```gdscript
func get_template(monster_id: String) -> Dictionary
func get_template_stats(monster_id: String, level: int = 1, nature_id: String = "") -> Dictionary

func get_instance_view(instance_id: String) -> Dictionary
func get_instance_views(instance_ids: Array) -> Array
func get_owned_instance_views(filters: Dictionary = {}) -> Array
func get_species_album_view(monster_id: String) -> Dictionary
func get_battle_unit_from_instance(instance_id: String) -> Dictionary

func get_monster_art(monster_id: String, usage: String = "battle") -> String
func validate_monster_catalog() -> Dictionary
```

### 怪物视图数据格式

```gdscript
{
	"instanceId": "m_1700000000000_ab12cd",
	"monsterId": "monster_001",
	"name": "小火龙",
	"templateName": "小火龙",
	"element": "fire",
	"rarity": 2,
	"level": 12,
	"exp": 40,
	"nature": "brave",
	"natureName": "勇敢",
	"capturedAt": 1700000000000,
	"stats": {
		"hp": 376,
		"maxHP": 376,
		"atk": 103,
		"def": 63,
		"spd": 28
	},
	"skill": { "name": "火焰冲击", "cost": 8, "multiplier": 2.5 },
	"leaderSkill": "",
	"evolution": { "level": 16, "target": "monster_006" },
	"art": {
		"battle": "res://assets/images/battle/monsters/monster_001_fire_lizard.png",
		"team": "res://assets/images/battle/monsters/monster_001_fire_lizard.png",
		"ranch": "res://assets/images/battle/monsters/monster_001_fire_lizard.png",
		"album": "res://assets/images/battle/monsters/monster_001_fire_lizard.png",
		"result": "res://assets/images/battle/monsters/monster_001_fire_lizard.png"
	}
}
```

### `MonsterDb` 优化

`MonsterDb` 保持为静态模板库，但需要明确只做以下事情：

- 保存怪物模板：编号、名称、属性、稀有度、基础数值、技能、进化、Boss 技能。
- 提供 `get_monster(monster_id)`、`get_all()`、`has_monster(monster_id)`。
- 提供 `get_monster_stats(monster_id, level, nature_id)` 计算模板在指定等级/性格下的属性。
- 提供 `validate_templates()` 检查进化目标、技能字段、基础数值字段是否完整。

不做：

- 不保存玩家持有状态。
- 不保存 `instanceId`。
- 不保存队伍或牧场状态。
- 不直接决定 UI 用哪张图。

注意：`growth_system.gd` 中如仍引用 `MonsterDB`，应统一成实际类名 `MonsterDb` 或通过新的 `MonsterService` 访问。

### `MonsterArtDB` 优化

当前 `MonsterArtDB` 只有 `BATTLE_PORTRAITS`，需要升级为多用途资产映射：

```gdscript
const MONSTER_ART := {
	"monster_001": {
		"battle": "res://assets/images/battle/monsters/monster_001_fire_lizard.png",
		"team": "res://assets/images/battle/monsters/monster_001_fire_lizard.png",
		"ranch": "res://assets/images/battle/monsters/monster_001_fire_lizard.png",
		"album": "res://assets/images/battle/monsters/monster_001_fire_lizard.png",
		"result": "res://assets/images/battle/monsters/monster_001_fire_lizard.png"
	}
}
```

接口建议：

```gdscript
static func get_art_path(monster_id: String, usage: String = "battle") -> String
static func get_art_bundle(monster_id: String) -> Dictionary
static func has_art(monster_id: String, usage: String = "battle") -> bool
static func validate_art_coverage(monster_ids: Array) -> Dictionary
```

Fallback 规则：

1. 优先使用指定 usage 的资源。
2. 如果没有，回退到 `battle`。
3. 如果仍没有，UI 使用 `emoji` 或通用占位图。
4. 校验工具输出缺失资源列表，不阻塞运行。

### 数据校验工具

新增一个轻量校验入口，建议放在 `MonsterService.validate_monster_catalog()`：

检查项：

- `MonsterDb` 中每个怪物都有 `id/name/element/rarity/baseHP/baseATK/baseDEF/baseSPD/skill`。
- 所有 `evolution.target` 都存在于 `MonsterDb`。
- 队长技能 ID 存在于 `LeaderSkillDb`。
- `MonsterArtDB` 至少覆盖 starter、当前可捕获怪物、当前关卡敌人和 Boss。
- 资产路径存在于 `ResourceLoader`。
- 玩家队伍和牧场引用的 `instanceId` 都能在怪物池中找到。

该校验可以作为开发期测试或调试命令，不必每次启动强制执行。

### 调用方改造方向

重构后，场景层尽量不要直接组合 `MonsterDb + SaveManager + MonsterArtDB`。

示例：

```gdscript
# 旧：场景自己拼
var monster := MonsterDb.get_monster_stats(monster_id, level, nature)
var art := MonsterArtDB.get_battle_portrait_path(monster_id)

# 新：统一获取
var view := MonsterService.get_instance_view(instance_id)
var monster := view["stats"]
var art := view["art"]["team"]
```

战斗入口：

```gdscript
var battle_unit := MonsterService.get_battle_unit_from_instance(instance_id)
```

图鉴入口：

```gdscript
var album_view := MonsterService.get_species_album_view(monster_id)
```

---

## 迁移策略

迁移在 `SaveManager.get_player()` 或首次读取怪物池时触发，建议加版本字段：

```gdscript
player["monsterPoolVersion"] = 1
```

### 迁移输入

旧结构：

- `player.captured: Array[String]`
- `player.pokedex: Dictionary[monsterId -> { level, exp, nature }]`
- `team.data: { leader, member1, member2 }`，值为 `monster_id`
- `ranch.data.slots[].monster_id`

### 迁移规则

1. 遍历旧 `captured`，为每个 `monster_id` 创建一个实例。
2. 如果 `pokedex[monster_id]` 存在，则迁移 `level`、`exp`、`nature`。
3. 旧数据无法区分同名多只，因此每个旧物种只迁移为一只实例。
4. 迁移旧队伍：
   - 将队伍槽位中的 `monster_id` 映射到对应新 `instanceId`。
   - 如果没有找到实例，则创建一个迁移实例。
5. 迁移旧牧场：
   - 将 `slots[].monster_id` 映射到新 `instanceId`。
   - 保留 `placed_at`。
6. 保留旧字段一段时间用于回退和兼容，但新逻辑不再写入旧 `captured` / `pokedex`。

### 默认新存档

新档初始化时直接创建三只 starter 实例，并用它们的 `instanceId` 初始化队伍：

- `monster_001`
- `monster_002`
- `monster_003`

---

## 改动文件清单

| 文件 | 改动类型 | 说明 |
|------|---------|------|
| `src/core/monster_pool.gd` | 新增 | 怪物实例管理工具模块 |
| `src/core/monster_service.gd` | 新增 | 统一怪物模板、实例、数值和资产视图 |
| `src/core/save_manager.gd` | 修改 | 接入怪物池、迁移旧存档、提供实例接口 |
| `src/data/monster_art_db.gd` | 修改 | 从单一战斗头像映射升级为多场景资产映射 |
| `src/ui/scene/scene_ranch.gd` | 修改 | 牧场列表和槽位改用实例 ID，加入进化入口 |
| `src/ui/scene/scene_team.gd` | 修改 | 从怪物池选择实例入队 |
| `src/ui/scene/scene_album.gd` | 修改 | 图鉴改为物种展示，移除进化按钮 |
| `src/ui/scene/scene_result.gd` | 修改 | 捕获成功时每次新增怪物实例 |
| `src/ui/scene/scene_battle.gd` | 修改 | 战斗初始化读取队伍实例 |
| `src/battle/battle_manager.gd` | 修改 | 支持直接接收实例计算后的战斗数据 |
| `src/ui/scene/scene_battle_prepare.gd` | 修改 | 战前预览显示实例等级、性格和属性 |
| `src/ui/scene/scene_evolve.gd` | 调整或废弃 | 进化逻辑迁移到实例接口，入口从图鉴移到牧场 |
| `src/data/monster_db.gd` | 小调整 | 维持模板属性计算，不负责实例存储 |
| `src/growth/growth_system.gd` | 修改 | 统一 `MonsterDb` 引用或改走 `MonsterService` |
| `tests/*` | 修改/新增 | 覆盖迁移、重复捕获、队伍、牧场、战斗经验回写 |

---

## 场景改造细节

### 牧场 `scene_ranch.gd`

目标：

- 显示所有怪物实例，而不是物种 ID 列表。
- 5 个槽位保存 `instance_id`。
- 同一种怪物的两只实例可以分别出现在列表中。
- 同一个实例不能同时放入多个牧场槽位。
- 选中槽位中的实例后显示进化按钮。
- 进化时调用 `SaveManager.evolve_instance(instance_id)`，保留同一个 `instanceId`。

UI 展示建议：

- 卡片主标题：物种名
- 副信息：`Lv.X`、性格、战力
- 同名多只可用捕获时间、性格或短实例编号区分

### 组队编队 `scene_team.gd`

目标：

- 队伍槽位保存 `instanceId`。
- 同一个实例不能重复编入。
- 同一物种的不同实例可以同时入队，除非后续设计显式禁止。
- 列表展示实例的性格和实际属性。

保存逻辑：

- `save_team()` 写入 `instanceId`。
- 战力计算调用 `get_instance_stats(instance_id)`。

### 图鉴 `scene_album.gd`

目标：

- 只展示物种维度的解锁状态。
- 移除进化按钮和跳转进化逻辑。
- 未捕获物种显示基础数值。
- 已捕获物种显示：
  - 已拥有数量
  - 代表实例的性格和实际数值
  - 可选：简短实例列表预览

不在图鉴里执行进化、升级、队伍、牧场等操作。

### 战斗 `scene_battle.gd` / `battle_manager.gd`

目标：

- 战斗队伍从 `team.data` 的 `instanceId` 读取。
- 进入 `BattleManager` 前转换为战斗用 Dictionary：
  - `instanceId`
  - `id` / `monsterId`
  - `name`
  - `level`
  - `nature`
  - `hp/maxHP/atk/def/spd`
  - `skill`
  - `leaderSkill`
- 战斗内伤害、技能、显示继续使用战斗 Dictionary。
- 战斗结算通过 `instanceId` 回写经验。

推荐 `BattleManager.init()` 新增兼容入口：

```gdscript
func init_with_player_team(player_team_stats: Array, enemy_monster_ids: Array, p_level: int, e_level: int, s_data = null, s_id: String = "") -> void
```

旧 `init(player_monster_ids, ...)` 可保留给测试或敌人初始化使用。

### 结算捕获 `scene_result.gd`

当前逻辑会阻止重复捕获同一种怪物。重构后应改为：

```gdscript
if _captured and not _capture_target.is_empty():
	var instance := _storage.add_monster_instance(_capture_target["id"], {
		"source": "capture",
		"level": max(1, _battle_result.get("enemyLevel", 1))
	})
```

不再判断 `captured_list.has(monster_id)`。

---

## 优先级

### P0 - 数据结构与迁移

1. 新建 `monster_pool.gd`。
2. 新建 `monster_service.gd` 或在 `MonsterPool` 中先提供统一怪物视图接口。
3. 在 `save_manager.gd` 增加怪物池读写和实例接口。
4. 实现旧存档迁移。
5. 新增迁移、重复捕获、怪物目录校验测试。

### P1 - 核心玩法接入

1. 改造 `scene_team.gd`，队伍保存实例 ID。
2. 改造 `scene_ranch.gd`，槽位保存实例 ID。
3. 改造 `scene_result.gd`，捕获成功每次新增实例。
4. 改造战斗初始化和结算经验回写。
5. 场景层改走统一怪物视图，减少直接拼装 `MonsterDb` / `MonsterArtDB` / `SaveManager`。

### P2 - 图鉴与进化整理

1. 简化 `scene_album.gd`，移除进化按钮和数据操作。
2. 将进化入口迁移到牧场。
3. 将进化执行改为 `evolve_instance(instance_id)`。
4. 将图鉴详情改走 `get_species_album_view(monster_id)`。

### P3 - 兼容清理与体验打磨

1. 更新战前准备页、成就统计和其它读取 `captured` 的 UI。
2. 将旧 `captured` / `pokedex` 写入点全部下线。
3. 增加同名多实例的 UI 区分信息。
4. 补齐 `MonsterArtDB` 的多场景资源映射和 fallback。
5. 增加 `validate_monster_catalog()` 开发期检查。

---

## 验收标准

1. 旧存档启动后自动迁移，原有三只初始怪物、队伍和牧场槽位不丢失。
2. 捕获两只同名怪物后，怪物池中出现两个不同 `instanceId`，且性格/捕获时间可不同。
3. 牧场能看到同名两只怪物，并可把不同实例分别放入不同槽位。
4. 组队编队能选择任意实例，同一实例不能重复入队。
5. 同一物种的不同实例可以分别获得经验、升级和进化。
6. 牧场中进化某只实例后，该实例的 `instanceId` 不变，`monsterId` 更新为进化目标。
7. 队伍中的实例进入战斗时使用自己的等级、性格和属性。
8. 战斗胜利后经验回写到参战实例，而不是回写到物种 ID。
9. 图鉴只显示物种信息和拥有状态，不提供进化按钮。
10. 未捕获物种显示基础数值，已捕获物种显示代表实例的实际数值和拥有数量。
11. UI 和战斗场景可以通过统一怪物视图获取模板、实例、数值、性格和美术资产。
12. 怪物资产查询支持 battle/team/ranch/album/result 用途，并有稳定 fallback。
13. 目录校验能列出缺失美术、无效进化目标、无效队长技能和断开的实例引用。

---

## 风险与注意事项

- 这是一次存档模型重构，必须先写迁移测试，再改 UI。
- 旧接口短期保留，但所有新写入必须走实例接口。
- `monsterId` 和 `instanceId` 命名要严格区分，避免把实例 ID 传给 `MonsterDb.get_monster()`。
- 战斗结果中的 `playerTeam` 应带 `instanceId`，否则结算无法精确给某只实例加经验。
- 图鉴验收不要再要求“图鉴网格展示两只同名怪物”；图鉴应展示物种，牧场和队伍展示实例。
- `stats` 不建议持久化缓存，除非后续证明属性计算成为瓶颈。
- `MonsterArtDB` 当前覆盖远少于 `MonsterDb`，补齐前必须保证 fallback 不影响运行。
- 统一视图层不要反向写存档，写操作仍由 `SaveManager` / `MonsterPool` 负责。

---

## 备注

- 不改动消除、伤害、BOSS 技能等战斗核心规则。
- 不改动道具/商店等无关系统，除进化消耗道具接口外。
- 本方案允许分阶段落地，但 P0 完成前不建议先改 UI。
