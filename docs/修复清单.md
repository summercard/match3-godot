# 修复清单

更新时间：2026-05-18

## 已完成

- [x] P0 结算奖励写入顺序：先计算奖励，再保存金币、玩家经验、怪物经验、捕获、道具和统计。
- [x] P0 战斗结果字段兼容：补齐并归一化 `stageId/stage_id`、`playerTeam/player_team`、`turnCount/turn_count`、`stageRewards/stage_rewards`、`totalDamageDealt/total_damage_dealt`。
- [x] P0 牧场放置入口：怪物 ID 改为字符串，放置、移除、领取后统一刷新槽位、列表、详情和收取按钮。
- [x] P0 怪物图片统一：新增 `MonsterArtDB`，队伍、战斗准备、战斗、结算共用一份怪物头像映射。
- [x] P0 战斗输入：修复 viewport 鼠标派发不能触发有效交换的问题。
- [x] P0 测试存档隔离：测试脚本默认使用 `user://test_save_game.cfg`，并支持 `MATCH3_SAVE_PATH` 指定存档路径。
- [x] P0 主流程调试日志清理：移除结算、存档、战斗、主界面的高频 debug 输出。
- [x] P1 扫荡奖励：从固定奖励改为按关卡奖励、星级倍率和扫荡倍率计算。
- [x] P1 商店经济：移除金币购买金币道具的套利路径。
- [x] P1 进化石投放：补齐初始怪物进化石的自然掉落路径。
- [x] P1 教程闭环：启动页按存档进入教程或主界面，教程进度可恢复，完成或跳过后返回主界面。
- [x] P1 玩家主动技能：满能量后保留充能，点击我方怪物卡片可释放技能、造成伤害并扣除能量。
- [x] P1 牧场存档 schema：SaveManager 统一保存 `monster_id/placed_at/unlocked_slots`，读取兼容旧 `monsterId/placedAt/unlockedSlots` 和秒级时间戳。
- [x] P1 数值体验：第一章奖励、玩家升级曲线、怪物战斗经验、牧场挂机收益已校准，前期两次首关胜利可触发明确升级反馈。
- [x] P2 战斗表现：补齐战斗 shake、敌我受击闪烁、敌方攻击目标反馈，并保留 combo 文本反馈。
- [x] P2 UI 文本适配：新增 `BattleUIFeedback`，统一 draw UI 的文本测量、截断、换行和阴影绘制入口。
- [x] P2 场景拆分：新增 `BattleInputMapper` 和 `BattleUIFeedback`，先拆出战斗输入映射、方向判断、文本绘制和反馈计算。
- [x] P2 测试清理：`battle_input_test.gd` 改为 input-only 测试路径并释放测试根节点，退出时不再出现 ObjectDB leak warning。
- [x] P3 战斗场景深拆：新增 `BattleAnimationController`、`BattleBoardRenderer`、`BattleCombatantRenderer`、`BattleFlowController`，拆出动画状态推进、棋盘绘制、角色卡绘制和战斗流程出口。
- [x] P4 战斗规则深拆：新增 `BattleMatchRules` 和 `BattleHazardRules`，拆出 match 上下文、特殊消除移除、毒雾扩散/伤害、毒雾清理、锁链解锁，并保留 `battle_fx_requested` 表现资源升级出口。

## 验收记录

- [x] `git diff --check` 通过。
- [x] `p0_smoke_test.gd` 通过。
- [x] `battle_input_test.gd` 通过，退出时无 ObjectDB leak warning。
- [x] `tutorial_flow_test.gd` 通过，覆盖首次进入、进度恢复、完成回主界面、跳过回主界面。
- [x] `active_skill_test.gd` 通过，覆盖未满能量不可释放、满能量直接释放、点击卡片释放。
- [x] `ranch_schema_test.gd` 通过，覆盖旧牧场字段迁移、毫秒时间戳归一、挂机经验领取。
- [x] `early_growth_test.gd` 通过，覆盖第一章奖励、两次首关升级、怪物早期升级、牧场收益可见。
- [x] P3 深拆后 `scene_battle.gd` 回归通过，主场景文件降至 2493 行，新增 4 个战斗组件承接职责。
- [x] `battle_rules_test.gd` 通过，覆盖特殊消除规则、移除计数、毒雾伤害、毒雾清理、锁链解锁。
- [x] P4 深拆后 `scene_battle.gd` 回归通过，主场景文件降至 2280 行，新增 2 个战斗规则组件承接职责。

## 待修复

- [ ] P3 表现资源升级：将当前 draw UI 的基础闪烁和震动替换为可配置动效、音效和特效资源。
- [ ] P5 战斗规则收口：继续把 Boss 阶段转换、状态效果视觉映射、捕获 inline 流程从 `scene_battle.gd` 迁移到独立组件。
