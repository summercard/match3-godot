# 1.3.5 版本修改内容记录（Godot 本地版）

> 版本：v1.3.5  
> 开始日期：2026-07-27  
> 适用工程：Godot 本地版  
> 当前状态：制作中

## 记录规则

- 本文件作为本次 1.3.5 制作对话的版本修改主记录。
- 本对话中产生的代码、资源、场景、数据、测试与文档修改，都必须在同一轮工作中同步记录到本文件。
- 未实际修改的盘点发现标为“发现/待办”，不得写成已修复或已验收。
- 验收状态沿用 1.3.2 口径：逻辑修改需有自动化证据，视觉修改需有运行画面或截图证据；未完成对应验证前不标记为通过。

## 一、精灵立绘资产治理

- 完成现有精灵立绘资产盘点：普通精灵 `monster_001` 至 `monster_103` 共 103 张，Boss `monster_boss_001` 至 `monster_boss_008` 共 8 张，合计 111 张正式物种立绘。
- 确认普通精灵正式运行目录为 `assets/images/monsters/monster/`，Boss 正式运行目录为 `assets/images/monsters/boss/`。
- 确认统一代码映射位于 `src/data/monster_art_db.gd`；当前每个物种只登记 `battle` 路径，`team`、`ranch`、`album`、`result` 均自动回退复用该立绘。
- 现有 `美术开发/` 下只有战局道具与属性图标代入表，没有精灵立绘专用表，因此新建 [精灵立绘资产代入表](../美术开发/精灵立绘资产代入表.md)，逐项记录 111 个物种的名称、属性、运行路径、九宫格汇总图、中央代码映射行与当前接入状态。
- 管理表同时记录 3 个旧敌人兼容别名、各正式界面的动态调用位置，以及绕过统一映射的场景直接绑定。
- 盘点发现 `src/ui/controllers/stage_select_logic.gd:192-199` 的旧关卡地图兜底仍包含 8 个不存在的 Boss 文件路径。本轮只登记风险，没有修改运行代码；后续应改为按 Boss 物种 ID 读取统一映射。
- 首轮盘点阶段没有修改精灵 PNG、Godot 场景或运行逻辑；随后按本记录第二节开始制作开局三精灵 idle 动画。

## 二、开局三精灵 16 帧 idle 循环动画

### 2.1 制作对象与规格

`src/core/monster_pool.gd` 中登记的开局三精灵为：

| ID | 名称 | 原始立绘 | 正式序列帧 |
| --- | --- | --- | --- |
| `monster_002` | 草兔兔 | `assets/images/monsters/monster/monster_002.png` | `assets/images/monsters/monster/monster_002/idle/idle_000.png`—`idle_015.png` |
| `monster_053` | 信使龟 | `assets/images/monsters/monster/monster_053.png` | `assets/images/monsters/monster/monster_053/idle/idle_000.png`—`idle_015.png` |
| `monster_093` | 火焰犬 | `assets/images/monsters/monster/monster_093.png` | `assets/images/monsters/monster/monster_093/idle/idle_000.png`—`idle_015.png` |

- 每只精灵 16 帧，共新增 48 张运行 PNG。
- 单帧 256×256、RGBA 透明背景，命名从 `idle_000.png` 连续到 `idle_015.png`。
- 当前播放速度为 `8 FPS`，一次完整循环约 2 秒。制作中曾按用户要求试用 4 FPS（相对原 8 FPS 放慢 2 倍），实机观感偏卡后调整为相对原速放慢 1.5 倍；用户随后反馈 5.33 FPS 偏卡，已恢复普通 8 FPS。
- 动作必须来自角色内部部件的逐帧形变，不得只对整张立绘做位移或缩放；主体身份特征、水平中心和脚底基准保持不变。
- 最终 16 帧按豆包 Seedance 2.0 Mini 四秒视频的时间顺序等间隔抽取；相邻采样间隔为 6—7 个 24 FPS 原始视频帧，`idle_015 → idle_000` 同样保持一个采样间隔并由播放端永久循环。

### 2.2 image-2 调用与正式帧选择

- 第一轮分别以三张原始立绘调用 image-2，制作 4×4 idle 动势参考；草兔兔和火焰犬末排出现裁切，动作变化也不足。第一轮没有作为最终运行帧，仅保留在：
  - `美术开发/元素提取/精灵动画/monster_002/idle_image2_motion_reference.png`
  - `美术开发/元素提取/精灵动画/monster_053/idle_image2_motion_reference.png`
  - `美术开发/元素提取/精灵动画/monster_093/idle_image2_motion_reference.png`
- 用户复核指出第一版只是整体上下浮动，不符合“动态序列帧”。随后重新调用 image-2 制作第二版 4×4 动画表，要求每格保留安全边距并产生真实部件动作：
  - 草兔兔：双耳折动、完整眨眼、前爪交替抬起、叶领压缩、叶尾与粉花跟随。
  - 火焰犬：双眼眨眼、耳朵压低/抬起、前爪抬起、胸毛呼吸、头顶火焰和尾焰逐帧变形。
  - 信使龟：眨眼、头部收放、前后足换重心、帽翼/邮包/信封联动、尾巴与气泡变化。
- 第二版 image-2 源表及透明工作表保存在各精灵目录的 `idle_image2_animation_sheet_v2.png` 和 `idle_image2_animation_sheet_v2_rgba.png`。
- 重写 `scripts/build_monster_idle_frames.py`：从第二版 image-2 透明工作表识别 16 个角色连通区域，把分离的气泡和火焰碎片归入对应帧；统一脚底基准和可见面积，消除合成表本身的大小/位置漂移，同时保留眨眼、耳朵、四肢、尾巴、火焰和配件的真实形变。
- 草兔兔的粉花与洋红键色接近，标准去色会误删花瓣；生成脚本增加保留粉色细节的颜色距离遮罩与反合成处理。火焰犬和信使龟继续使用 imagegen 自带去键色脚本并执行 1 px 边缘收缩。
- image-2 工作表继续置于被忽略的 `美术开发/元素提取/` 工作区，不进入游戏运行包；正式 48 帧位于 `assets/images/monsters/monster/` 并由 Godot 导入。
- 用户继续复核后要求以视频产生更连贯的内部动画。最终运行帧已由第三节的“四只原版精灵 2×2 四宫格 → 豆包四秒视频 → 拆格 → 通道制作”流程完全覆盖；第二版 image-2 动画表和单角色视频只保留为制作过程，不再是当前运行帧来源。

### 2.3 数据登记与循环播放

- 修改 `src/data/monster_art_db.gd`：
  - 保留三只精灵及旧别名 `enemy_003` 的原始静态 PNG 映射，作为旧接口和缺帧兼容回退。
  - 新增 `MONSTER_ANIMATIONS`，登记 `idle` 的路径模板、16 帧、约 5.33 FPS 与 `loop=true`。
  - 新增动画查询、完整帧路径、正负索引取模和动画存在性接口。
- 新增 `src/ui/components/monster_idle_animator.gd`：
  - 为 `TextureRect` 绑定逐帧播放器，按约 5.33 FPS 更新纹理并按 16 帧取模无限循环。
  - 共享已加载帧缓存，避免同一精灵在多个界面重复读取资源。
  - 提供 `texture_at_time()`，供签到、新手教程等自绘界面按同一时间规则取得当前帧。
  - 每次绑定从 `idle_000`—`idle_015` 随机选择起始帧；签到和教程的自绘路径也保存各自随机偏移，避免所有实例固定从第一帧同时播放。
  - 未登记动画的精灵不创建播放器，继续显示原静态 PNG。
- 制作过程中曾验证 Godot `AnimatedTexture` 方案；当前 Godot 4.6.2 运行中不能可靠推进 `current_frame`，所以对应临时 `.tres` 与生成脚本已删除，不作为 v1.3.5 运行方案。

### 2.4 游戏界面接入

以下正式界面已改为在遇到 `MONSTER_ANIMATIONS` 已登记精灵时直接播放 `idle_000`—`idle_015`；未登记精灵仍沿用静态立绘：

| 场景 | 修改文件 | 接入位置 |
| --- | --- | --- |
| 开始页 | `src/ui/scene/scene_start.gd` | 三只固定开局精灵；只播放各自逐帧动画，不再叠加整张立绘上下浮动 |
| 商店 | `src/ui/scene/scene_shop_gui.gd` | 草兔兔吉祥物 |
| 正式战斗 | `src/ui/scene/scene_battle_gui.gd` | 敌我双方存活立绘；倒下时解绑以保留幽灵演出 |
| 战前准备 | `src/ui/scene/scene_battle_prepare.gd` | 队伍卡与敌方卡 |
| 战斗结算 | `src/ui/scene/scene_result_gui.gd` | 捕获成功与队伍经验卡 |
| 队伍/旅店 | `src/ui/scene/scene_team_gui.gd` | 编队槽、精灵分页列表及异步加载回退 |
| 图鉴 | `src/ui/scene/scene_album_gui.gd` | 图鉴卡、详情立绘和进化预览 |
| 农场/课堂/社交 | `src/ui/scene/scene_ranch_gui.gd` | 列表卡、观察立绘和进化预览 |
| 关卡地图 | `src/ui/scene/scene_stage_select_gui.gd` | 招牌怪物与 Boss 台座立绘 |
| 邮箱 | `src/ui/scene/scene_mailbox.gd` | 冒险精灵与来信精灵 |
| 每日签到 | `src/ui/scene/scene_sign_in.gd` | 自绘草兔兔吉祥物 |
| 新手教程 | `src/ui/scene/scene_tutorial.gd` | 自绘队伍与固定草兔兔示例 |

### 2.5 管理表与版本控制

- 更新 `美术开发/精灵立绘资产代入表.md`，增加三精灵动画目录、image-2 动势参考、运行规格、代码入口、接入场景和验证结果。
- 修改 `.gitignore`，仅放行 `美术开发/精灵立绘资产代入表.md` 进入版本控制；其余源美术与一次性 image-2 参考继续忽略。
- Godot 为新增运行 PNG、动画组件和测试生成对应 `.import` / `.uid` 旁车文件，随资源一并保留。

### 2.6 验证结果

- `godot --headless --path . --import`：通过，Godot 4.6.2 完成 48 张序列帧和新脚本导入，无解析错误。
- 图片 QA：48/48 均为 256×256 RGBA；透明通道存在；主体边界未触碰画布；统一脚底基准；三组均为 16 个不同文件，位置/尺寸归一后仍各有 16 个不同画面，证明变化来自内部姿态而非只移动整张图。
- 更新 `tests/monster_idle_animation_test.gd`：验证静态回退、16 帧完整路径、命名、尺寸、约 5.33 FPS、`loop=true`、随机起始帧、自绘随机偏移、正负索引取模和第 15 帧回到第 0 帧，并要求每组 16 个原始帧全部不同、位置/尺寸归一后至少 12 个姿态仍不同。
- 已运行开始页、商店、战斗、倒下幽灵、战前/结算、队伍、队伍延迟加载、图鉴、农场、进化、关卡地图、地图立绘、邮箱、签到、新手教程与开局数值回归；最终复核 17/17 均通过。战前/结算、农场进化、邮箱与教程测试退出时仍报告既有资源占用提示，但测试退出码均为 0。
- 第二版真实动作帧覆盖完成后再次运行动画专项以及开始页、商店、战斗、战前/结算、队伍、图鉴、农场、关卡地图、邮箱、签到、教程共 12 项最终回归，12/12 通过；同时执行工程启动检查与 `git diff --check`，均通过。
- 四宫格视频正式帧覆盖后再次执行 `godot --headless --path . --import`、`monster_idle_animation_test.gd` 和开始页至教程的 11 项场景回归：动画专项与 11/11 场景测试全部通过。战前/结算和教程退出时仍有既有资源占用提示，但退出码均为 0。

## 修改流水

| 日期 | 序号 | 范围 | 修改文件 | 结果 | 验证 |
| --- | --- | --- | --- | --- | --- |
| 2026-07-27 | 1 | 版本记录基线 | `docs/1.3.5版本修改内容记录.md` | 建立 1.3.5 持续修改记录 | 文档结构与 1.3.2 记录口径对照 |
| 2026-07-27 | 2 | 精灵立绘资产治理 | `美术开发/精灵立绘资产代入表.md` | 建立 111 个正式物种、3 个兼容别名、动态/直接引用位置和缺失兜底路径的管理表 | 文件数量、中央映射和引用位置静态核对 |
| 2026-07-27 | 3 | image-2 动势探索 | `美术开发/元素提取/精灵动画/{monster_002,monster_053,monster_093}/idle_image2_motion_reference.png` | 形成三只精灵 16 帧 idle 动势参考；因裁切/造型漂移只作参考 | 人工查看三张合成表 |
| 2026-07-27 | 4 | 第一版正式序列帧 | `scripts/build_monster_idle_frames.py`；`assets/images/monsters/monster/{monster_002,monster_053,monster_093}/idle/` | 曾从原图生成 3×16 张呼吸/浮动帧 | 后续用户复核判定“只有上下浮动，不是角色动画”，已被第二版完全替换 |
| 2026-07-27 | 5 | 动画数据与播放器 | `src/data/monster_art_db.gd`；`src/ui/components/monster_idle_animator.gd` | 登记动画元数据并实现约 5.33 FPS 无限循环、静态回退和自绘取帧 | `monster_idle_animation_test.gd` 通过 |
| 2026-07-27 | 6 | 游戏界面接入 | 12 个 `src/ui/scene/*.gd` 文件 | 开始、商店、战斗、战前、结算、队伍、图鉴、农场、地图、邮箱、签到、教程接入动画 | 对应场景回归测试通过 |
| 2026-07-27 | 7 | 自动化 | `tests/monster_idle_animation_test.gd` | 新增序列完整性与 15→0 循环回归 | `[MonsterIdleAnimation] OK` |
| 2026-07-27 | 8 | 表格入库 | `.gitignore`；`美术开发/精灵立绘资产代入表.md` | 只放行精灵立绘管理表，继续忽略源美术输出 | `git status --untracked-files=all` 仅显示管理表 |
| 2026-07-27 | 9 | 动画纠偏 | 第二版 `idle_image2_animation_sheet_v2*.png`；`scripts/build_monster_idle_frames.py`；48 张 `idle_*.png`；`tests/monster_idle_animation_test.gd` | 完全替换整体位移/缩放帧，改为眨眼、耳朵、四肢、尾巴/火焰/配件真实形变序列 | 三张 4×4 透明预览人工复核；内部姿态差异自动检查通过 |
| 2026-07-27 | 10 | 最终回归 | 三组正式 `idle` 序列帧及 12 个已接入界面 | 确认第二版真实动作资源可导入、可循环并保持原有界面逻辑 | 动画专项与 11 项场景回归 12/12 通过；工程启动和 `git diff --check` 通过 |
| 2026-07-27 | 11 | 单角色视频流程探索 | 三组 `doubao_video/image2_magenta_input*.png`；`scripts/build_monster_idle_from_videos.py` | 建立 image-2 玫红底、豆包四秒视频、16 帧抽取和透明化流程；草兔兔与火焰犬单独成片并完成拆帧，信使龟被豆包多次误判为侵权/违规 | 两段单角色视频规格检查及草兔兔/火焰犬接触表通过；随后被四宫格最终流程覆盖 |
| 2026-07-27 | 12 | 原版角色四宫格兼容方案 | `美术开发/元素提取/精灵动画/starter_idle_quad_2x2/image2_original_characters_quad_2x2.png`；`scripts/build_starter_idle_quad.py` | 按用户要求不修改信使龟形象；以草兔兔、原版信使龟、火焰犬和辅助 `monster_003` 组成 2×2 玫红底输入，成功生成四秒视频 | 输入图 2064×2064；成片 720×720、24 FPS、97 帧、4.096 秒；豆包未再触发版权拦截 |
| 2026-07-27 | 13 | 四宫格拆帧与正式替换 | `scripts/build_starter_idle_quad.py`；三组正式 `idle/idle_000.png`—`idle_015.png` | 自动识别视频白色分隔带，拆出前三格，去玫红/白边/灰影和水印残留，输出 3×16 张 256×256 RGBA 正式帧；右下辅助格不进入运行包 | 三张 4×4 接触表人工通过；动画专项通过；开始页至教程 11/11 场景回归通过 |
| 2026-07-27 | 14 | 播放节奏与错峰 | `src/data/monster_art_db.gd`；`src/ui/components/monster_idle_animator.gd`；`scene_sign_in.gd`；`scene_tutorial.gd`；动画专项测试 | 先将 8 FPS 放慢 2 倍至 4 FPS，随后按用户体验反馈改为放慢 1.5 倍的约 5.33 FPS；所有实例改为随机起始帧 | Godot 导入和 `[MonsterIdleAnimation] OK` |
| 2026-07-27 | 15 | 第二批动画输入 | `scripts/build_monster_idle_quad_batches.py`；`monster_idle_quad_batches/group_01`—`group_03` | 生成三张 2064×2064 原版角色 2×2 玫红底输入，每组右下格作为辅助格 | 三张输入图人工检查，无角色裁切 |
| 2026-07-27 | 16 | 豆包三组视频 | `monster_idle_quad_batches/group_01`—`group_03/idle_seedance_2_mini_4s.mp4` | 以 Seedance 2.0 Mini、1:1、4 秒、固定镜头和内部待机动作生成并下载三段视频 | 每段 720×720、24 FPS、97 帧、4.041667 秒 |
| 2026-07-27 | 17 | 新增九只精灵动画 | `monster_001`、`003`—`010` 的 `idle/`；`MonsterArtDB.MONSTER_ANIMATIONS` | 新增 9×16 张透明序列帧并接入通用动画播放器；三只右下辅助精灵不在对应批次导出 | 144/144 为 256×256 RGBA；动画专项通过 |
| 2026-07-27 | 18 | 透明通道复核 | `scripts/build_monster_idle_quad_batches.py`；三组 `qa/` 接触表与 5.33 FPS GIF | 对九组逐帧生成接触表；对 `monster_010` 的不稳定中性边缘碎屑增加时间中值清理后重建 | 九张接触表人工复核；`monster_010` 残留显著清除 |
| 2026-07-27 | 19 | 临时全图鉴验收 | `src/ui/controllers/album_logic.gd`；`tests/album_gui_scene_test.gd` | 启用既有 `DEBUG_UNLOCK_ALL_ALBUM_FOR_QA`，只在图鉴运行时派生全收录列表，不改写玩家存档 | `[AlbumGuiSceneTest] passed`；待用户验收后恢复 `false` |
| 2026-07-27 | 20 | 最终回归 | 12 只动画、播放器、全图鉴临时模式与 11 个正式界面 | 确认 5.33 FPS、随机起始帧、192 张正式帧和全图鉴验收模式可共同运行 | Godot 导入、动画专项和开始页至教程 11/11 场景回归通过；既有资源占用提示不影响退出码 |
| 2026-07-27 | 21 | 欢迎页动作去重 | `src/ui/scene/scene_start.gd`；`tests/start_gui_scene_test.gd` | 移除欢迎页三只宠物整张立绘的正弦上下浮动，只保留各自 16 帧内部待机动画；宝石浮动和按钮呼吸不变 | `[StartGuiScene] OK`，额外验证 0.24 秒内三只宠物节点位置保持不变 |
| 2026-07-27 | 22 | 再追加三组视频 | `monster_idle_quad_batches/group_04`—`group_06`；`scripts/build_monster_idle_quad_batches.py` | 使用原版立绘完成三张 2064×2064 四宫格、三段 Seedance 2.0 Mini 四秒视频；正式目标为 `monster_011`—`016`、`018`—`020` | 三段视频均为 720×720、24 FPS、97 帧；`group_04` 为 4.041667 秒，`group_05`、`group_06` 为 4.096009 秒 |
| 2026-07-27 | 23 | 禁止跨批重复与拆帧 QA | `scripts/build_monster_idle_quad_batches.py`；`group_04`—`group_06/qa/` | 用户指出辅助精灵跨批复用后，`group_06` 在提交前改为此前未出现的 `018`、`019`、`020`、`021`；后续四宫格四只均须从未出现精灵中选取。九只正式目标输出 144 张透明帧，并对瞬时色偏、鸟足误抠和飞散轮廓做定点清理 | 九张 4×4 接触表与九个 5.33 FPS GIF 人工复核；累计 21 只、336 张正式 idle 帧 |
| 2026-07-27 | 24 | 动画登记与完整回归 | `src/data/monster_art_db.gd`；`tests/monster_idle_animation_test.gd`；11 项界面测试 | 为新增九只精灵登记 16 帧、约 5.33 FPS、随机起始、无限循环；欢迎页保持固定位置 | Godot 导入、`[MonsterIdleAnimation] OK`、开始页至教程 11/11 场景回归和 `git diff --check` 全部通过 |
| 2026-07-27 | 25 | 第三轮分组确认与输入 | `scripts/build_monster_idle_quad_batches.py`；`group_07`—`group_09/original_characters_quad_2x2.png` | 经用户先行确认，选定从未出现在历史四宫格且没有 `idle` 目录的 `monster_022`—`033`；三组每格均为正式目标，共 12 只，不再设置辅助格 | 12/12 原始 PNG 存在；三张 2064×2064 输入人工复核无裁切；右下正式目标预留水印安全区 |
| 2026-07-27 | 26 | 第三轮豆包视频 | `group_07`—`group_09/idle_seedance_2_mini_4s.mp4` | 使用 Seedance 2.0 Mini、1:1、4 秒、固定镜头生成三段四宫格内部待机动画；`group_08` 首次平台生成失败且未扣额度，按原组原参数重试成功 | 三段均为 720×720、24 FPS、97 帧、4.096009 秒 |
| 2026-07-27 | 27 | 十二只拆帧与透明 QA | `scripts/build_monster_idle_quad_batches.py`；`monster_022`—`033/idle/`；三组 `qa/` | 每只输出 16 张 256×256 RGBA 透明 PNG；替换瞬时分色帧，稳定 `monster_022` 背包边缘附件，移除 `monster_029` 孤立杂点，并对右下正式目标执行水印安全区去色 | 12 张 4×4 接触表与 12 个 5.33 FPS GIF 人工复核；192/192 帧完成 |
| 2026-07-27 | 28 | 第三轮登记与回归 | `src/data/monster_art_db.gd`；`tests/monster_idle_animation_test.gd`；11 项界面测试 | 登记 `monster_022`—`033` 的 16 帧、约 5.33 FPS、随机起始和无限循环；累计 33 只、528 张正式 idle 帧 | Godot 导入、`[MonsterIdleAnimation] OK` 和开始页至教程 11/11 场景回归通过 |
| 2026-07-27 | 29 | 第四轮三组确认与输入 | `scripts/build_monster_idle_quad_batches.py`；`group_10`—`group_12/original_characters_quad_2x2.png` | 用户确认 `monster_034`—`045` 为下一轮三组共 12 个正式目标；三组四格均正式导出，并为右下目标预留动态水印安全区 | 12/12 原图存在且无已有 `idle/`；三张 2064×2064 输入图已生成 |
| 2026-07-27 | 30 | 第四轮第十组生成与拆帧 | `group_10/idle_seedance_2_mini_4s.mp4`；`monster_034`—`037/idle/` | Seedance 2.0 Mini 以 1:1、4 秒生成 `group_10`；输出 4×16 张透明帧、接触表与 5.33 FPS GIF，并登记动画运行数据 | 视频为 720×720、24 FPS、97 帧、4.096009 秒；接触表人工复核通过 |
| 2026-07-27 | 31 | 第四轮额度阻塞 | `group_11`、`group_12` 工作目录与管理表 | `group_11` 提交后豆包提示“今日视频生成免费次数用完”；未生成视频、未扣费以外的任何套餐，不替换模型、不修改角色；`group_12` 因同一额度限制未提交 | 平台提示截图复核；保留三张输入图，等待用户提供可用额度或后续恢复 |
| 2026-07-27 | 32 | 第十组运行接入与回归 | `src/data/monster_art_db.gd`；`tests/monster_idle_animation_test.gd`；11 项界面测试 | 为 `monster_034`—`037` 登记 16 帧、约 5.33 FPS、随机起始与无限循环；累计 37 只、592 张正式 idle 帧 | Godot 导入、`[MonsterIdleAnimation] OK` 和开始页至教程 11/11 场景回归通过 |
| 2026-07-27 | 33 | idle 播放速度恢复 | `src/data/monster_art_db.gd`；`src/ui/components/monster_idle_animator.gd`；`tests/monster_idle_animation_test.gd`；本表及资产管理表 | 用户反馈约 5.33 FPS 偏卡；37 只已接入精灵统一恢复普通 8 FPS（16 帧约 2 秒一循环），保留随机起始帧和无限循环 | Godot 导入、`[MonsterIdleAnimation] OK`、开始页至教程 11/11 场景回归通过；既有资源占用提示不影响退出码 |
| 2026-07-27 | 34 | 动画生产流程 Skill | `~/.codex/skills/godot-monster-idle-pipeline/SKILL.md`；`agents/openai.yaml` | 固化原版立绘四宫格、Seedance 2.0 Mini 固定镜头视频、16 帧透明拆分、跨批去重、8 FPS 随机循环、Godot 接入、QA 与版本记录流程 | `quick_validate.py` 通过；Skill 已可被 Codex 自动发现 |
| 2026-07-28 | 35 | 动画生产流程 Skill 更名 | `~/.codex/skills/sc-art-portrait-animation/SKILL.md`；`agents/openai.yaml` | Skill 内部标识按命名规范改为 `sc-art-portrait-animation`，界面名称改为用户指定的“sc-art-立绘动画制作” | `quick_validate.py` 通过；旧目录已迁移，不保留重复 Skill |
| 2026-07-28 | 36 | 动画生产流程 Skill 通用化 | `~/.codex/skills/sc-art-portrait-animation/SKILL.md`；`agents/openai.yaml` | 适用范围扩展为任意项目的“立绘转逐帧动画”；2×2 仅保留为可选工作方式，Godot、16 帧、256×256 和 8 FPS 均改为按项目规格执行 | `quick_validate.py` 通过；更新后的 Skill 已可自动发现 |
| 2026-07-28 | 37 | 下一轮三组目标确认 | `scripts/build_monster_idle_quad_batches.py`；`美术开发/精灵立绘资产代入表.md` | 延续已受额度阻塞的 `group_11`（038—041）与 `group_12`（042—045），新增 `group_13`（046—049）；12 只均无 idle 目录且未在历史四宫格出现 | 静态路径、动画登记和历史批次去重检查通过；待生成输入、视频、拆帧和运行时回归 |
| 2026-07-28 | 38 | 第十三组输入与额度复核 | `scripts/build_monster_idle_quad_batches.py`；`group_13/original_characters_quad_2x2.png`；资产管理表 | 为 `monster_046`—`049` 生成 2064×2064 原版角色四宫格输入；人工复核无裁切。豆包当前继续提示当日免费视频次数耗尽 | 输入图尺寸/构图检查通过；未购买套餐、未改用其它模型；三组均待可用额度后续做 |
| 2026-07-28 | 39 | 七组动画目标扩展 | `scripts/build_monster_idle_quad_batches.py`；`美术开发/精灵立绘资产代入表.md` | 用户将本轮扩展为 `group_11`—`group_17` 共 28 只；新增 050—052、054—066 共 16 只，显式跳过已有 idle 的 053 | 28/28 静态立绘存在、均无 idle 目录、与历史四宫格不重复；待生成输入、视频、拆帧和运行时回归 |
| 2026-07-28 | 40 | 第十四至十七组输入图 | `scripts/build_monster_idle_quad_batches.py`；`group_14`—`group_17/original_characters_quad_2x2.png`；资产管理表 | 输出四张 2064×2064 原版角色玫红四宫格输入；`group_14` 人工复核无裁切，其余组按同一已验证脚本生成 | 四张输入图尺寸检查、脚本语法检查和 `git diff --check` 通过；待 Seedance 2.0 Mini 视频生成 |
| 2026-07-28 | 41 | 透明通道白色镂空修复 | `scripts/build_monster_idle_from_videos.py`；`scripts/build_starter_idle_quad.py`；`scripts/build_monster_idle_quad_batches.py`；`tests/test_monster_idle_chroma_key.py`；37 只既有 `idle/` | 删除会误伤眼白、牙齿、白毛和浅色配件的“边缘白色删除”与 GrabCut 前景分类；只对高置信度玫红键色全局去除，对压缩偏色仅清除边缘连通背景；用保留的开局与 `group_01`—`10` 视频重建 37×16 张正式帧 | 4 项玫红键控回归测试通过；592/592 张旧帧重建通过；静态立绘与 `idle_000` 对照人工复核浅色结构完整 |
| 2026-07-28 | 42 | 第十一至十七组豆包视频 | `monster_idle_quad_batches/group_11`—`group_17/idle_seedance_2_mini_4s.mp4` | 使用 Seedance 2.0 Mini、1:1、4 秒、固定镜头生成七段原版角色四宫格待机视频；`group_11` 的一次错误 10 秒结果单独标记并排除，重新生成正式 4 秒结果；`group_14` 首次被平台误判疑似违规且未扣额度，改用中性动作描述原组重试成功 | 七段正式视频均为 720×720、24 FPS、97 帧、4.096009 秒；错误 10 秒文件不参与拆帧或运行时 |
| 2026-07-28 | 43 | 七组拆帧与透明 QA | `scripts/build_monster_idle_quad_batches.py`；`monster_038`—`052`、`054`—`066/idle/`；七组 `qa/` | 以修正后的玫红键控输出 28×16 张 256×256 RGBA 透明序列帧；每只同步生成 4×4 接触表与 8 FPS 循环 GIF；保护眼白、牙齿、白毛、浅色腹部和配件 | 448/448 运行帧、28/28 接触表、28/28 GIF 规格检查通过；七组接触表人工复核角色特征、透明边缘与内部动作 |
| 2026-07-28 | 44 | 七组运行接入与回归 | `src/data/monster_art_db.gd`；`tests/monster_idle_animation_test.gd`；Godot 资源导入；11 项界面测试 | 为 038—052、054—066 登记 16 帧、8 FPS、随机起始和无限循环；累计 65 只、1040 张正式 idle 帧 | Godot 4.6.2 导入、`[MonsterIdleAnimation] OK`、抠图单元测试 4/4 及开始页至教程 11/11 场景回归通过 |
| 2026-07-28 | 45 | 第十八组四只精灵动画 | `group_18/`；`monster_067`—`070/idle/`；`src/data/monster_art_db.gd`；动画专项与界面回归 | 去重选择哧溜豹、哧溜溜豹、哧溜溜溜溜豹和冰帝小企鹅；以 2064×2064 玫红四宫格生成 Seedance 2.0 Mini 1:1 四秒视频，输出 4×16 张透明帧、4 张接触表及 4 个 8 FPS GIF，并登记 8 FPS、随机起始、无限循环。页面首个媒体地址误指历史视频，接触表 QA 发现角色重复后未接入；打开新预览取得正确媒体源并覆盖临时错误输出 | 正式视频为 720×720、24 FPS、97 帧、4.096009 秒；64/64 帧规格及白色细节人工复核通过；累计 69 只、1104 帧；Godot 导入、`[MonsterIdleAnimation] OK`、抠图 4/4 与 11/11 场景回归通过 |
| 2026-07-28 | 46 | QA GIF 帧率精确化 | `scripts/build_monster_idle_quad_batches.py`；`group_18/qa/*_idle_8fps.gif` | GIF 延时只能使用 10 毫秒单位，原 125 毫秒会被编码为固定 120 毫秒；改为逐帧交替 120/130 毫秒，使 16 帧平均延时精确为 125 毫秒，与游戏 8 FPS 一致 | 4 个 GIF 均为 16 帧、256×256，延时序列为 120/130 毫秒交替；平均 125 毫秒检查通过 |
| 2026-07-28 | 47 | 第十九、二十组输入与豆包视频 | `scripts/build_monster_idle_quad_batches.py`；`group_19`、`group_20/original_characters_quad_2x2.png`；两段 `idle_seedance_2_mini_4s.mp4` | 去重选择 `monster_071`—`078`，生成两张 2064×2064 原版角色玫红四宫格；在既有豆包对话中分别提交全新指令，以 Seedance 2.0 Mini、1:1、4 秒、固定镜头生成内部待机动画 | 8/8 静态立绘存在、均无既有 `idle/` 且未在历史批次出现；两段视频均为 720×720、24 FPS、97 帧、4.096009 秒 |
| 2026-07-28 | 48 | 八只拆帧与透明 QA | `monster_071`—`078/idle/`；`group_19`、`group_20/qa/` | 每只输出 `idle_000.png`—`idle_015.png`，共 128 张 256×256 RGBA 透明序列帧；同步生成 8 张接触表与 8 个平均精确 8 FPS GIF | 128/128 帧规格、透明通道和命名通过；GIF 均为 16 帧且延时按 120/130 毫秒交替；接触表人工确认眨眼/表情等内部动作及眼白、白毛、花冠、冰晶完整 |
| 2026-07-28 | 49 | 第十九、二十组运行接入与回归 | `src/data/monster_art_db.gd`；`tests/monster_idle_animation_test.gd`；资产管理表与本记录 | 为 `monster_071`—`078` 登记 16 帧、8 FPS、随机起始和无限循环；累计 77 只普通精灵、1232 张正式 idle 帧 | Godot 资源导入、`[MonsterIdleAnimation] OK`、抠图单元测试 4/4 与开始页至教程 11/11 场景回归通过；既有退出时资源占用提示不影响退出码 |
| 2026-07-28 | 50 | 第二十一、二十二组目标与额度阻塞 | `scripts/build_monster_idle_quad_batches.py`；`group_21`、`group_22/original_characters_quad_2x2.png`；资产管理表 | 去重选定 `monster_079`—`086` 并生成两张 2064×2064 原版角色玫红四宫格。`group_21` 以全新指令提交 Seedance 2.0 Mini、1:1、4 秒后，平台立即提示“今日视频生成免费次数用完”；未生成视频、未购买套餐、未换模型。`group_22` 因同一额度限制暂未提交 | 8/8 静态立绘存在、均无 `idle/` 且未在历史四宫格出现；两张输入图人工复核无裁切。当前仅完成输入，未拆帧、未接入运行时，累计数量仍为 77 只、1232 帧 |
| 2026-07-29 | 51 | 第二十一、二十二组额度恢复与视频生成 | `group_21`、`group_22/idle_seedance_2_mini_4s.mp4` | 额度恢复后不刷新页面、不沿用旧失败消息；分别重新上传两张原版角色四宫格并输入两条全新待机指令，使用 Seedance 2.0 Mini、1:1、4 秒、固定镜头生成两段视频 | 两段正式视频均为 720×720、24 FPS、97 帧、4.096009 秒；页面均明确提示“你的视频生成好了” |
| 2026-07-29 | 52 | 八只拆帧与保白透明 QA | `monster_079`—`086/idle/`；`group_21`、`group_22/qa/` | 以修正后的玫红键控输出 8×16 张 256×256 RGBA 透明序列帧；同步生成 8 张 4×4 接触表和 8 个平均精确 8 FPS 循环 GIF | 128/128 帧尺寸、RGBA、命名通过；GIF 均为 16 帧且按 120/130 毫秒交替；接触表确认眨眼、翅膀、触角、毛发、睡帽和星月等内部动作，眼白、白毛、枕头高光及星月浅色细节未被抠空 |
| 2026-07-29 | 53 | 第二十一、二十二组运行接入与回归 | `src/data/monster_art_db.gd`；`tests/monster_idle_animation_test.gd`；资产管理表与本记录 | 为 `monster_079`—`086` 登记 16 帧、8 FPS、随机起始和无限循环；累计 85 只普通精灵、1360 张正式 idle 帧 | Godot 4.6.2 资源导入、`[MonsterIdleAnimation] OK`、抠图单元测试 4/4 与开始页至教程 11/11 场景回归通过 |
| 2026-07-29 | 54 | 第二十三至二十六组目标与输入图 | `scripts/build_monster_idle_quad_batches.py`；`group_23`—`group_26/original_characters_quad_2x2.png`；资产管理表 | 去重选定 `monster_087`—`092`、`094`—`103` 共 16 只；为大体型角色加入 6% 安全留边，右下正式目标继续使用水印安全布局，并仅在角色外清除 `monster_095` 原图角落的旧生成水印 | 16/16 静态立绘存在、均无 `idle/`，未在开局四宫格或 `group_01`—`22` 出现；四张输入图均为 2064×2064，人工复核无跨格、无角色裁切、无残留文字 |
| 2026-07-29 | 55 | 第二十三至二十六组豆包视频 | `group_23`—`group_26/idle_seedance_2_mini_4s.mp4` | 在既有豆包对话中分别上传四张原版角色四宫格并提交四条全新指令，使用 Seedance 2.0 Mini、1:1、4 秒、固定镜头生成角色内部待机动画；未沿用旧失败消息 | 四段正式视频均为 720×720、24 FPS、97 帧、4.096009 秒；页面四组均提示“你的视频生成好了” |
| 2026-07-29 | 56 | 十六只拆帧与保白透明 QA | `monster_087`—`092`、`094`—`103/idle/`；`group_23`—`group_26/qa/` | 使用高置信度玫红键控与边缘连通背景清理，输出 16×16 张 256×256 RGBA 透明序列帧；每只同步生成 4×4 接触表和平均精确 8 FPS 循环 GIF | 256/256 帧命名、尺寸和透明通道通过；16/16 GIF 为 16 帧且延时按 120/130 毫秒交替；接触表人工确认眨眼、表情、翼/尾/花瓣/火焰/鱼鳍等内部动作，眼白、犬类白毛、獠牙和浅色腹部完整 |
| 2026-07-29 | 57 | 第二十三至二十六组运行接入与回归 | `src/data/monster_art_db.gd`；`tests/monster_idle_animation_test.gd`；资产管理表与本记录 | 为 `monster_087`—`092`、`094`—`103` 登记 16 帧、8 FPS、随机起始和无限循环；累计 101 只普通精灵、1616 张正式 idle 帧 | Godot 4.6.2 资源导入、`[MonsterIdleAnimation] OK`、抠图单元测试 4/4 与开始页至教程 11/11 场景回归通过；Godot 默认日志轮转在快速连续启动时崩溃，改用每项独立 `/tmp` 日志后复跑通过 |
| 2026-07-29 | 58 | 两组 Boss 目标与输入图 | `scripts/build_monster_idle_quad_batches.py`；`group_27`、`group_28/original_characters_quad_2x2.png`；资产管理表 | 将 `monster_boss_001`—`008` 按 ID 顺序拆成两组，每组 4 只；脚本增加 Boss 源目录、运行目录和安全留边支持，并只在主体外清除 `monster_boss_007` 原图旧生成水印 | 8/8 Boss 静态立绘存在、均无 `idle/`；两张输入图均为 2064×2064，人工复核无重复、跨格、角色裁切或残留文字；待生成视频、拆帧和运行时接入 |
| 2026-07-29 | 59 | 两组 Boss 豆包视频 | `group_27`、`group_28/idle_seedance_2_mini_4s.mp4` | 在既有豆包对话中分别重新上传两张 Boss 四宫格并提交两条全新指令，使用 Seedance 2.0 Mini、1:1、4 秒、固定镜头生成角色内部待机动画 | 两段正式视频均为 720×720、24 FPS、97 帧、4.096009 秒；页面两组均提示“你的视频生成好了” |
| 2026-07-29 | 60 | 八个 Boss 拆帧与保白透明 QA | `monster_boss_001`—`008/idle/`；`group_27`、`group_28/qa/` | 以修正后的玫红键控输出 8×16 张 256×256 RGBA 透明序列帧；同步生成 8 张 4×4 接触表与 8 个平均精确 8 FPS 循环 GIF | 128/128 帧尺寸、RGBA、命名通过；GIF 均为 16 帧且按 120/130 毫秒交替；逐只确认花瓣/藤蔓、白毛、浅色腹部、鹿角枝叶、晶体、水浪、九尾、虚空圆环与火焰完整，主体无跨格或整体漂浮 |
| 2026-07-29 | 61 | Boss 运行接入与完整回归 | `src/data/monster_art_db.gd`；`tests/monster_idle_animation_test.gd`；资产管理表与本记录 | 为 `monster_boss_001`—`008` 登记 16 帧、8 FPS、随机起始和无限循环；累计 109 个动画角色、1744 张正式 idle 帧 | Godot 4.6.2 资源导入、109 个角色动画专项、抠图单元测试 4/4 与开始页至教程 11/11 场景回归全部通过 |

## 待办与验收

| 项目 | 当前状态 | 下一步 |
| --- | --- | --- |
| 111 张正式立绘文件存在性 | 已盘点 | 增加可重复运行的资产覆盖测试时再纳入自动化 |
| 五类 usage 的独立构图 | 当前全部复用 `battle` | 仅在确有界面构图需求时拆分，避免重复资产 |
| 关卡地图旧 Boss 兜底路径 | 发现 8 个缺失路径 | 后续改为统一 ID 映射并补回归测试 |
| 101 只普通精灵 16 帧 idle | 开局 3 只、`group_01`—`group_26` 的正式目标均已完成；共 1616 帧，当前 8 FPS、随机起始帧、循环播放 | 继续由用户在全图鉴模式验收节奏、角色一致性和透明通道 |
| 其余 2 只普通精灵动画 | `monster_017`、`monster_021` 尚未制作；8 个 Boss 已全部完成 | 继续按“不重复精灵”规则跳过历史辅助格目标，除非用户明确允许重新制作 |
| 临时全图鉴验收开关 | 已开启；不写入存档 | 用户完成验收后把 `DEBUG_UNLOCK_ALL_ALBUM_FOR_QA` 恢复为 `false` 并还原对应测试断言 |

## 三、豆包视频转序列帧流程（已完成）

### 3.1 流程调整

- 用户复核认为 image-2 直接生成 4×4 动画表仍容易出现逐格造型漂移，要求改为“image-2 单张玫红底 → 豆包 Seedance 2.0 Mini 四秒视频 → 均匀抽取 16 帧 → 玫红转透明”的视频流程。
- 三只精灵分别以原始正式立绘为引用再次调用 image-2，只制作单张视频输入图，不直接生成序列帧；输出保存在：
  - `美术开发/元素提取/精灵动画/monster_002/doubao_video/image2_magenta_input.png`
  - `美术开发/元素提取/精灵动画/monster_053/doubao_video/image2_magenta_input.png`
  - `美术开发/元素提取/精灵动画/monster_093/doubao_video/image2_magenta_input.png`
- image-2 输出的玫红底存在轻微色差和渐变，因此只选择与画布边缘连通的玫红区域并校正为统一 `#ff00ff`，生成同目录的 `image2_magenta_input_flat.png`；连通区域限制用于保护草兔兔的粉色花朵。
- 草兔兔和火焰犬的单角色视频生成、下载和拆帧曾成功；信使龟使用同样参数时多次触发豆包“疑似包含侵权 / 违规内容”误判。
- 曾调用 image-2 生成一张更原创化的信使龟安全变体，但用户明确要求不得修改角色形象；该变体随即移出工程且从未进入豆包正式四宫格、视频或运行序列帧。
- 最终按用户指定改为 2×2 四宫格输入，所有角色均使用项目原有形象：
  - 左上：`monster_002` 草兔兔。
  - 右上：`monster_053` 原版信使龟。
  - 左下：`monster_093` 火焰犬。
  - 右下：`monster_003` 辅助叶系精灵，只用于降低单角色误判并承接右下角水印，不导出运行帧。
- 四宫格输入为 `美术开发/元素提取/精灵动画/starter_idle_quad_2x2/image2_original_characters_quad_2x2.png`，尺寸 2064×2064，四格之间保留 16 px 纯玫红间隔。

### 3.2 豆包参数与当前进度

- 豆包视频模式：`Seedance 2.0 Mini`、`1:1`、`4s`。
- 提示词共同约束：固定镜头、固定构图、脚底不动、禁止主体整体平移/上下漂浮/旋转/缩放，只允许眨眼、耳朵/四肢/尾巴/火焰/配件等内部待机动作，首尾自然衔接，背景保持纯玫红。
- 四宫格视频已成功生成并保存为 `美术开发/元素提取/精灵动画/starter_idle_quad_2x2/idle_seedance_2_mini_4s.mp4`。
- 成片规格：H.264、720×720、24 FPS、97 个视频帧、4.096 秒、670716 字节；另含 AAC 音轨，拆帧时不使用音频。
- 视觉复核确认四格镜头固定、角色不跨格；草兔兔和信使龟有完整眨眼，火焰犬头焰/尾焰逐帧变化，四只角色均存在内部动作。

### 3.3 视频拆帧脚本

- 新增 `scripts/build_monster_idle_from_videos.py`：
  - 从每个四秒视频按总帧数均匀抽取 16 帧。
  - 对高置信度玫红键色执行全局透明化，清除被枝叶/肢体围住的封闭背景；对视频压缩后的宽松玫红范围只清除与画布边缘连通的区域，使用软透明边缘和去玫红处理。
  - 明确保留所有白色与浅色角色细节；回归测试覆盖纯白眼睛、浅色腹部和被轮廓围住的玫红背景。
  - 将整幅固定镜头画面统一缩放到 256×256，不逐帧重新居中或独立缩放角色。
  - 输出继续沿用 `idle_000.png`—`idle_015.png`，并在覆盖前验证 16 帧唯一性、位置/尺寸归一后的内部姿态差异、RGBA 与透明四角。
- 新增最终使用的 `scripts/build_starter_idle_quad.py`：
  - 可重复构建 2064×2064 四宫格上传图。
  - 在每个视频帧中央自动检测豆包生成的白色纵横分隔带，因此不依赖假定的平均 360×360 裁切。
  - 只导出左上、右上、左下三格；右下辅助格和其水印全部丢弃。
  - 对每格执行玫红键控、边缘连通压缩残色清理、底部水印/地面残影清理和 256×256 固定画面缩放；不再按白色或 GrabCut 前景分类制作透明通道。
  - 每组覆盖前要求 16 帧全部不同、位置/尺寸归一后至少 12 个内部姿态不同、四角透明。

### 3.4 最终输出与验收

- 正式运行目录已覆盖：
  - `assets/images/monsters/monster/monster_002/idle/`
  - `assets/images/monsters/monster/monster_053/idle/`
  - `assets/images/monsters/monster/monster_093/idle/`
- 每组 `idle_000.png`—`idle_015.png` 均为 256×256 RGBA 透明 PNG；三组共 48 帧，当前由约 5.33 FPS、随机起始帧、`loop=true` 播放器无限循环。
- 人工接触表复核：草兔兔的眨眼/表情/叶片变化、原版信使龟的眨眼/重心/气泡变化、火焰犬的眨眼/火焰变化均清晰；没有把右下辅助角色或豆包水印带入正式帧。
- 自动验证：Godot 资源导入通过；`monster_idle_animation_test.gd` 通过；开始页、商店、战斗、战前/结算、队伍、图鉴、农场、地图、邮箱、签到、教程 11/11 场景回归通过。

## 四、追加三组、九只精灵动画（已完成）

### 4.1 分组与正式输出

| 批次 | 正式导出（左上/右上/左下） | 右下辅助格 | 视频 |
| --- | --- | --- | --- |
| `group_01` | `monster_001` 大眼蜗、`monster_003` 兔八子、`monster_004` 草兔王 | `monster_005` 风铃猫头鹰 | `monster_idle_quad_batches/group_01/idle_seedance_2_mini_4s.mp4` |
| `group_02` | `monster_005` 风铃猫头鹰、`monster_006` 飓风猫头鹰、`monster_007` 草洋洋 | `monster_008` 猫头鹰王 | `monster_idle_quad_batches/group_02/idle_seedance_2_mini_4s.mp4` |
| `group_03` | `monster_008` 猫头鹰王、`monster_009` 花洋洋、`monster_010` 绵洋洋 | `monster_011` 水珠花 | `monster_idle_quad_batches/group_03/idle_seedance_2_mini_4s.mp4` |

- 三段均使用原始正式立绘组成 2×2 玫红底输入，没有调用模型重画或替换角色外观。
- 豆包参数统一为 `Seedance 2.0 Mini`、`1:1`、`4 秒`；要求固定镜头、角色留在原格、禁止整体漂浮/缩放，只做眨眼、头部、耳翼、叶片、四肢、尾巴和配件的内部待机动作，并要求首尾循环。
- 三段下载视频均为 720×720、24 FPS、97 帧、4.041667 秒。
- `scripts/build_monster_idle_quad_batches.py` 可重建三张上传图，并从已下载视频等间隔抽取 16 帧、拆格、去玫红、制作透明通道、输出到同名精灵 `idle/`。
- 九只正式输出共 144 张 `256×256 RGBA PNG`；与开局三只合计 12 只、192 张正式 idle 帧。

### 4.2 画面与运行验收

- 每只精灵均生成 4×4 接触表和约 5.33 FPS GIF，保存在对应批次 `qa/` 目录；人工复核确认眨眼与内部部件动作清晰，主体保持在原位置，未把辅助格角色带入正式帧。
- `monster_010` 原始 PNG 自带中性条纹残留，视频中会形成不稳定边缘碎屑；批次脚本只对该角色的右侧/底部不稳定中性像素做时间中值替换，保留脸、羊角、叶片和身体动作，重建后的接触表通过人工复核。
- `MonsterArtDB.MONSTER_ANIMATIONS` 现登记 `monster_001`—`monster_010` 中除原有 `monster_002` 外的九只，以及开局 `monster_002`、`monster_053`、`monster_093`，共 12 只。
- Godot 资源导入、12 只动画专项测试和临时全图鉴场景测试通过；开始页、商店、战斗、战前/结算、队伍、图鉴、农场、关卡地图、邮箱、签到、新手教程共 11/11 场景回归通过。战前/结算、邮箱和教程退出时仍出现既有资源占用提示，但测试退出码均为 0。

## 五、临时全图鉴验收模式

- 按用户要求临时把 `src/ui/controllers/album_logic.gd` 的 `DEBUG_UNLOCK_ALL_ALBUM_FOR_QA` 设为 `true`。
- 开关只在图鉴载入时把 `_captured_ids` 派生为 111 个物种 ID，不调用存档写入，不增加持有实例，也不改变正常捕获、图鉴星星或邮箱奖励数据。
- `tests/album_gui_scene_test.gd` 临时同步验证 111/111 全收录。用户完成验收后必须把开关改回 `false`，并恢复“初始收录数小于总数”的正常模式断言。

## 六、再追加三组、九只精灵动画（已完成）

### 6.1 分组、去重规则与视频

| 批次 | 正式导出（左上/右上/左下） | 右下辅助格 | 视频规格 |
| --- | --- | --- | --- |
| `group_04` | `monster_011` 水珠花、`monster_012` 小喷嘴、`monster_013` 大喷嘴 | `monster_014` 矿鼠鼠 | 720×720、24 FPS、97 帧、4.041667 秒 |
| `group_05` | `monster_014` 矿鼠鼠、`monster_015` 矿伯鼠、`monster_016` 矿山老板 | `monster_017` 不高兴 | 720×720、24 FPS、97 帧、4.096009 秒 |
| `group_06` | `monster_018` 很不高兴、`monster_019` 特别不高兴、`monster_020` 迷路绵阳 | `monster_021` 还在迷路咩 | 720×720、24 FPS、97 帧、4.096009 秒 |

- `group_04` 的辅助 `monster_014` 曾在已经完成的 `group_05` 中成为正式目标，这是用户发现的画面重复来源；它没有被重复导出或覆盖同一套动画。
- 用户提出“下次不要重复精灵”时 `group_06` 尚未提交，因此立即取消原定的 `monster_017`，重排为此前未出现在任何四宫格中的 `monster_018`、`019`、`020`、`021`。
- 从 `group_06` 起建立强制生产规则：生成新四宫格前先核对本表，四个格子在批次内和跨历史批次都不得重复。辅助格一旦使用也视为“已出现”，不得在后续批次再次作为正式目标或辅助格。
- 三段均使用项目原始正式 PNG 合成 2064×2064 玫红底 2×2 输入，豆包参数保持 `Seedance 2.0 Mini`、`1:1`、`4 秒`、固定镜头、禁止整体漂浮/平移/旋转/缩放，只允许角色内部待机动作。

### 6.2 正式帧、清理与运行接入

- 新增正式动画：`monster_011`—`monster_016`、`monster_018`—`monster_020`，每只 `idle_000.png`—`idle_015.png`，共 144 张 256×256 RGBA 透明 PNG。
- `group_05` 与 `group_06` 的第 60 个原视频帧出现一次红/绿分色，抽帧脚本改取相邻且动作连续的第 61 帧；没有用重复帧或静态补帧。
- `group_06` 左上飞鸟的原第 12 帧发生灰色鸟足误抠，改取相邻完整的第 13 帧，并为该角色关闭会误判灰色喙/足的 GrabCut 精修；右上飞鸟只保留主透明连通体，清除脱离角色的细碎运动残影。
- `MonsterArtDB.MONSTER_ANIMATIONS` 新增九只登记，继续使用 `8 ÷ 1.5 ≈ 5.33 FPS`、`loop=true`、随机起始帧；当前总计 21 只精灵、336 张正式 idle 帧。
- 欢迎页三只宠物已移除整张立绘上下浮动，只播放自己的逐帧动画；宝石浮动与按钮呼吸仍保留。

### 6.3 验证

- `group_04`—`group_06` 的九只正式精灵均生成 4×4 接触表与 5.33 FPS 循环 GIF；人工复核角色身份、透明边缘、眨眼/翅膀/花瓣/水花/矿灯/四肢等内部动作。
- Godot 4.6.2 资源导入通过；动画专项验证 21 只精灵的尺寸、命名、16 帧唯一性、内部姿态差异、5.33 FPS、随机起始帧与 `idle_015 → idle_000` 循环。
- 开始页、商店、战斗、战前/结算、队伍、图鉴、农场、关卡地图、邮箱、签到和教程 11/11 回归通过；临时全图鉴仍保持开启且不写入玩家存档。
- `python3 -m py_compile` 与 `git diff --check` 通过；正式动画目录复核为 21 只、336 张帧图。

## 七、第三轮三组、十二只精灵动画（已完成）

| 批次 | 四只正式导出精灵 | 完成状态 |
| --- | --- | --- |
| `group_07` | `monster_022` 超级路痴羊、`monster_023` 小雨滴、`monster_024` 大雨滴、`monster_025` 暴雨来咯 | 视频、4×16 帧、透明通道、接触表与 GIF 完成 |
| `group_08` | `monster_026` 哔哩、`monster_027` 哔哩哔哩、`monster_028` 普拉哔哩、`monster_029` 小拳拳菇 | 首次平台生成失败后原参数重试；视频、4×16 帧、透明通道、接触表与 GIF 完成 |
| `group_09` | `monster_030` 拳菇王、`monster_031` 摇滚蝠、`monster_032` 叛逆蝠、`monster_033` 迪杰蝠 | 视频、4×16 帧、透明通道、接触表与 GIF 完成 |

- 本轮每组四格全部正式导出，因此三组准确计为 12 只精灵、192 张透明序列帧；与此前 21 只合计为 33 只、528 张正式帧。
- 去重检查覆盖开局四宫格及 `group_01`—`group_06`：`monster_022`—`033` 均为首次出现，后续批次继续从 `monster_034` 起按历史表排除已出现精灵。
- 三张上传图均为 2064×2064；三段 Seedance 2.0 Mini 视频均为 720×720、24 FPS、97 帧、4.096009 秒。`group_08` 首次返回“生成失败、额度未扣除”，未改变角色或参数，原样重试成功。
- 为避免豆包动态水印遮住右下正式目标，`group_07`—`group_09` 的右下角色在原格内统一缩小并向左上保留安全空白；拆帧时完成玫红转透明和右下安全区清理。
- 逐帧 QA 替换三段视频中第 60 帧的瞬时红绿分色；固定 `monster_022` 背包右缘附件，防止视频模型短暂把附件变成额外面孔；移除 `monster_029` 的孤立紫色细线。十二张接触表确认主体固定、身份特征保持、眨眼/表情/耳朵/水滴/闪电/拳套/翅膀/乐器等内部动作存在。
- `MonsterArtDB.MONSTER_ANIMATIONS` 已登记十二只新动画；Godot 资源导入、33 只动画专项和开始页至教程 11/11 场景回归通过。临时全图鉴验收模式继续开启且不写入玩家存档。

## 八、第四轮与十二组扩展精灵动画（制作中）

| 批次 | 四只正式导出精灵 | 当前进度 |
| --- | --- | --- |
| `group_10` | `monster_034` 路灯草、`monster_035` 路灯亮亮、`monster_036` 托尼、`monster_037` 时尚托尼 | 视频下载、4×16 帧、透明通道、接触表与 GIF 完成；已接入运行时 |
| `group_11` | `monster_038` 托尼大师、`monster_039` 幽幽鱼、`monster_040` 小木木、`monster_041` 阿木阿木 | 正式 4 秒视频、4×16 帧、透明通道、接触表与 8 FPS GIF 完成；已接入运行时 |
| `group_12` | `monster_042` 甲球、`monster_043` 刺球怪、`monster_044` 刺刺哥哥、`monster_045` 酒蝎 | 正式 4 秒视频、4×16 帧、透明通道、接触表与 8 FPS GIF 完成；已接入运行时 |
| `group_13` | `monster_046` 豪华酒蝎、`monster_047` 小岩球、`monster_048` 大岩球、`monster_049` 肖肖蜥 | 正式 4 秒视频、4×16 帧、透明通道、接触表与 8 FPS GIF 完成；已接入运行时 |
| `group_14` | `monster_050` 肖恩蜥、`monster_051` 沙漠粉狐、`monster_052` 沙漠魅影、`monster_054` 破浪鬼 | 首次平台误判后原图重试；正式 4 秒视频、4×16 帧、透明通道、接触表与 8 FPS GIF 完成；已接入运行时 |
| `group_15` | `monster_055` 超速闪电龟、`monster_056` 水泡泡、`monster_057` 啪噗、`monster_058` 光宝 | 正式 4 秒视频、4×16 帧、透明通道、接触表与 8 FPS GIF 完成；已接入运行时 |
| `group_16` | `monster_059` 宝霸、`monster_060` 水晶豹、`monster_061` 成年水晶豹、`monster_062` 海噜噜 | 正式 4 秒视频、4×16 帧、透明通道、接触表与 8 FPS GIF 完成；已接入运行时 |
| `group_17` | `monster_063` 深海噜噜、`monster_064` 椰果果、`monster_065` 大椰果果、`monster_066` 肥椰果 | 正式 4 秒视频、4×16 帧、透明通道、接触表与 8 FPS GIF 完成；已接入运行时 |
| `group_18` | `monster_067` 哧溜豹、`monster_068` 哧溜溜豹、`monster_069` 哧溜溜溜溜豹、`monster_070` 冰帝小企鹅 | 正式 4 秒视频、4×16 帧、透明通道、接触表与 8 FPS GIF 完成；旧媒体链接产生的重复角色临时输出被 QA 拦截并覆盖；已接入运行时 |
| `group_19` | `monster_071` 冰帝企鹅、`monster_072` 冰帝企鹅王、`monster_073` 噗尼、`monster_074` 噗尼蚌蚌 | 正式 4 秒视频、4×16 帧、透明通道、接触表与平均精确 8 FPS GIF 完成；已接入运行时 |
| `group_20` | `monster_075` 噗尼尼蚌蚌蚌、`monster_076` 小白、`monster_077` 九尾小白、`monster_078` 水晶小白 | 正式 4 秒视频、4×16 帧、透明通道、接触表与平均精确 8 FPS GIF 完成；已接入运行时 |
| `group_21` | `monster_079` 极光小熊、`monster_080` 五彩小可爱、`monster_081` 指路星、`monster_082` 启明星 | 正式 4 秒视频、4×16 帧、透明通道、接触表与平均精确 8 FPS GIF 完成；已接入运行时 |
| `group_22` | `monster_083` 萤火彗星、`monster_084` 瞌睡熊、`monster_085` 还在瞌睡熊、`monster_086` 赖床熊 | 正式 4 秒视频、4×16 帧、透明通道、接触表与平均精确 8 FPS GIF 完成；已接入运行时 |
| `group_23` | `monster_087` 小地龙、`monster_088` 成年地龙、`monster_089` 地龙领主、`monster_090` 夜来香 | 正式 4 秒视频、4×16 帧、透明通道、接触表与平均精确 8 FPS GIF 完成；已接入运行时 |
| `group_24` | `monster_091` 花满楼、`monster_092` 小凤仙、`monster_094` 熔岩犬、`monster_095` 熔岩蟹宝宝 | 角色外旧生成水印已清除；正式 4 秒视频、4×16 帧、透明通道、接触表与平均精确 8 FPS GIF 完成；已接入运行时 |
| `group_25` | `monster_096` 熔岩蟹、`monster_097` 生气野猪、`monster_098` 愤怒野猪、`monster_099` 热辣锅 | 正式 4 秒视频、4×16 帧、透明通道、接触表与平均精确 8 FPS GIF 完成；已接入运行时 |
| `group_26` | `monster_100` 火山烧、`monster_101` 炎鱼、`monster_102` 炎鱼宝宝、`monster_103` 斗炎鱼 | 正式 4 秒视频、4×16 帧、透明通道、接触表与平均精确 8 FPS GIF 完成；已接入运行时 |
| `group_27` | `monster_boss_001` 花草兽、`monster_boss_002` 我的刀盾、`monster_boss_003` 瑞幸、`monster_boss_004` 巨石王 | 正式 4 秒视频、4×16 帧、透明通道、接触表与平均精确 8 FPS GIF 完成；已接入运行时 |
| `group_28` | `monster_boss_005` 深海霸主、`monster_boss_006` 雪狐王、`monster_boss_007` 混沌虚空、`monster_boss_008` 山火巨灵 | 角色外旧生成水印已清除；正式 4 秒视频、4×16 帧、透明通道、接触表与平均精确 8 FPS GIF 完成；已接入运行时 |

- 去重检查覆盖开局四宫格及 `group_01`—`group_09`：`monster_034`—`045` 均为首次出现、均无现有 `idle/` 目录。
- 本轮继续采用 Seedance 2.0 Mini、1:1、4 秒、固定镜头、角色内部待机动作；`group_11`—`group_17` 每组四格均为正式目标，新增 28×16=448 张透明序列帧。
- 七段正式视频均为 720×720、24 FPS、97 帧、4.096009 秒。`group_11` 的错误 10 秒结果保存在 `idle_seedance_2_mini_10s_rejected.mp4`，明确不参与拆帧或游戏调用；正式输入使用同目录 `idle_seedance_2_mini_4s.mp4`。
- 修正后的抠图工具不再删除白色或运行 GrabCut，只全局删除高置信度玫红，并把压缩偏色限制为边缘连通背景；28 张接触表确认角色白色细节未被错误镂空。
- 为 28 只新增精灵登记普通 8 FPS、随机起始、无限循环；与既有 37 只合计 65 只、1040 张正式帧。Godot 导入、动画专项、抠图 4 项单元测试及开始页至教程 11/11 场景回归通过。
- `group_18` 继续按同一规格新增 4 只、64 张正式帧；正确视频与输入均为雪地精灵内容，白色身体、眼白和浅色雪具未被抠空。累计更新为 69 只、1104 张正式帧，Godot 导入、69 只动画专项和 11/11 界面回归通过。
- `group_19`、`group_20` 均使用全新未重复目标和全新豆包指令；两段正式视频均为 720×720、24 FPS、97 帧、4.096009 秒。新增 8 只、128 张正式帧，眼白、白毛、浅色花冠和冰晶结构保持完整。
- `group_21`、`group_22` 在额度恢复后分别使用重新上传的原图与全新指令生成，未沿用旧额度失败消息；两段正式视频均为 720×720、24 FPS、97 帧、4.096009 秒。
- 当前累计 85 只普通精灵、1360 张正式 idle 帧，统一为 8 FPS、随机起始和无限循环；Godot 导入、85 只动画专项、抠图 4 项单元测试及开始页至教程 11/11 场景回归通过。`monster_017`、`monster_021` 曾作为旧批次辅助格出现，仍继续跳过以遵守“不重复精灵”规则。
- `group_23`—`group_26` 已固定为剩余 16 只从未出现在历史四宫格的普通精灵；四张上传图加入安全留边并完成视觉复核。`monster_093` 已有正式动画，未重复选入。
- `group_23`—`group_26` 四段正式视频均为 720×720、24 FPS、97 帧、4.096009 秒；新增 16 只、256 张 256×256 RGBA 正式帧。接触表确认主体留在各自格内且存在眨眼、表情、龙翼/尾巴、花瓣/藤蔓、火焰、汤汁气泡及鱼鳍等内部动作，浅色结构未被误抠。
- 当前累计 101 只普通精灵、1616 张正式 idle 帧，统一登记为 8 FPS、随机起始帧和无限循环；Godot 导入、101 只动画专项、抠图 4 项单元测试与开始页至教程 11/11 场景回归通过。仅余 `monster_017`、`monster_021` 两只普通精灵因历史辅助格重复规则未制作。
- `group_27`、`group_28` 已完成全部 8 个 Boss：两段 Seedance 2.0 Mini 正式视频均为 720×720、24 FPS、97 帧、4.096009 秒，输出 128 张 256×256 RGBA 正式帧；`monster_boss_007` 的旧生成水印只在主体外清除。
- 8 张接触表确认 Boss 身份、格子位置和白色/浅色结构完整，存在眨眼、藤蔓、叶片、毛发、披风、晶体、水浪、尾巴、虚空圆环、火焰与烟气等内部动作，不使用整体上下漂浮；当前累计 109 个动画角色、1744 张正式 idle 帧。Godot 导入、109 个角色动画专项、抠图 4 项单元测试与 11/11 界面回归通过。
