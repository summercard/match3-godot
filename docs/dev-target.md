# 开发目标

## 创建时间
2026-05-17 14:15

## 最后更新
2026-05-17 14:47

## 当前循环：15（Plan阶段）

## 已完成功能审视结果

### 游戏循环视觉表现对比（sceneBattle.js vs scene_battle.gd）

| 游戏循环阶段 | 微信源码表现 | Godot 当前表现 | 差距等级 |
|-------------|-------------|---------------|---------|
| 宝石消除动画 | 外部 animation.js playEliminateEffect：放大+闪白→缩小+消失，per-gem 视觉状态 | 两段式消除 (ELIMINATE_PHASE1/PHASE2)，有粒子特效 | ✅ 已对齐 |
| 宝石脉动 | 未选中 0.85↔1.0 周期2s，选中 0.95↔1.0 周期1s | 相同实现 + idle 呼吸缩放 | ✅ 已对齐 |
| 伤害数字浮动 | FloatingTextManager：文字上飘 + 淡出（800ms），crit 弹跳效果 | 三阶段动画（pop→rise→fade），上飘30px + 淡出 + crit缩放 | ✅ 已对齐 |
| HP 条渐变 | enemyDisplayHP/playerDisplayHP：旧值→新值 easeOut 渐变 0.3s | HP条渐变已接入敌方和我方（preHP 作为动画起点） | ✅ 已对齐 |
| 敌方回合伤害定位 | 每个受击我方怪物精确定位浮动文字 + 受击闪烁 | per-target 定位已修复 ✅ | ✅ 已对齐 |
| 玩家卡片受击效果 | 黄色闪烁覆盖层 + HP 渐变动画 | filter 逻辑正确（f.get("isEnemy", false) 匹配玩家卡片） | ✅ 已对齐 |
| 连锁弹窗 | scale 0.5→1.2→1.0 + opacity 动画 | 相同三阶段动画 | ✅ 已对齐 |
| 屏幕震动 | 攻击命中震动 ±4px 200ms + 白闪 0.1s | 相同实现 + 棋盘震动 | ✅ 已对齐 |
| BOSS 阶段切换 | 闪白 + 震动 + 1.5s 提示框 + 重置棋盘 | 相同实现 | ✅ 已对齐 |
| 状态效果 DoT | 灼烧/中毒伤害飘字 + emoji + 受击闪烁 | 已有飘字 + emoji + 受击闪烁 | ✅ 已对齐 |

### 已完成深化（Cycle 14）
- ✅ **连击系统深化**：
  - 高连锁屏幕震动强化（≥4：震动0.5s + 闪白0.35s）
  - 连锁即时提示（≥2：棋盘上方金色"×N"上浮0.8s）
  - 连锁进度条（≥4：底部金色条显示"×N Chain!"）

### 已审视但无明显问题
- ✅ **收服特效**：CaptureEffect 成功/失败序列完整
- ✅ **特殊消除视觉反馈**：_trigger_special_elim 分时触发逻辑正确

### 待深化项
- ⚠️ **高连锁连锁特效（5+）**：背景色脉冲 + 粒子更密集
- ⚠️ **元素连锁光晕**：_trigger_element_glow 在 cross/L/T 触发时视觉可强化
- ⚠️ **元素连锁全屏波纹**：_trigger_element_ripple 效果可增强

## 本次目标
### 目标类型
深化已有 / 元素连锁视觉增强

### 功能名称
元素连锁光晕与波纹视觉强化

### 深化重点 / 具体任务
- [ ] 审视 `_trigger_element_glow` 当前实现，确认光晕颜色/持续时间是否匹配微信
- [ ] 审视 `_trigger_element_ripple` 当前实现，确认波纹颜色/持续时间/大小是否匹配微信
- [ ] 检查 `_element_glow` 和 `_element_ripple` 在 `draw()` 中的实际渲染逻辑
- [ ] 如有差距，对齐微信源码行为

### 涉及文件
- 修改：src/ui/scene/scene_battle.gd

### 验收标准
- 元素连锁光晕颜色和持续时间与微信版一致
- 元素连锁全屏波纹效果与微信版一致
- 不破坏现有棋盘震动和连锁进度条功能